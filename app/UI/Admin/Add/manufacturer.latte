{block content}
    <h1>Přidat výrobce</h1>

    {form addManufacturerForm}
        <div class="form-group">
            {label name}
            {input name}
            {if $form['name']->hasErrors()}
                <div class="form-error">{$form['name']->getError()}</div>
            {/if}
        </div>

        <div class="form-group">
            {input submit}
        </div>
    {/label}
    {/form}
{/block}